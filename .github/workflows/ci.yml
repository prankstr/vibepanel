name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: '-Dwarnings'

jobs:
  fmt:
    runs-on: ubuntu-latest
    name: Formatting
    steps:
      - uses: actions/checkout@v4
      - run: cargo fmt --check

  check:
    runs-on: ubuntu-latest
    container: rust:trixie
    name: Clippy & Test
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y \
            libgtk-4-dev \
            libgtk4-layer-shell-dev \
            libpulse-dev \
            libudev-dev \
            libdbus-1-dev \
            dbus-daemon
          rustup component add clippy
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test --verbose

  ui-regression-tests:
    runs-on: ubuntu-latest
    container: rust:trixie
    name: UI Regression Tests
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y \
            libgtk-4-dev \
            libgtk4-layer-shell-dev \
            libpulse-dev \
            libudev-dev \
            libdbus-1-dev \
            dbus-daemon \
            xvfb
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Run display-required UI regression tests
        run: scripts/run-ui-regression-tests.sh

  layer-shell-tests:
    runs-on: ubuntu-latest
    container: rust:trixie
    name: Layer-shell Tests (headless Sway)
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y \
            libgtk-4-dev \
            libgtk4-layer-shell-dev \
            libpulse-dev \
            libudev-dev \
            libdbus-1-dev \
            dbus-daemon \
            sway \
            grim \
            python3
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Run layer-shell contracts
        run: scripts/run-auto-hide-tests.sh

  font-check:
    runs-on: ubuntu-latest
    name: Font subset manifest
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/subset-font.sh --check
